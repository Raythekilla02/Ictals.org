Options -Indexes
RewriteEngine On

# Serve files directly
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# Serve directories directly
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Do NOT route through Laravel
RewriteRule ^ - [L]
